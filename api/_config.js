// INTERN settings — the only file to edit at launch. Empty = not set yet (the site shows it honestly).
module.exports = {
  name: 'INTERN',
  ticker: '$INTERN',
  ca: '',        // token mint once it exists
  x: '',         // project X handle, without @
  flagship: '',  // the flagship intern's public wallet (its desk is shown on the home page)
};
